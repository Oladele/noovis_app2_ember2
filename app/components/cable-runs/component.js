import Ember from 'ember';

const {
  computed
} = Ember;


export default Ember.Component.extend({
  // didReceiveAttrs() {
    // this._super(...arguments);
    // this.get('emberModel.sheets').then(model => {
      // return model.get('lastObject.cableRuns');
    // }).then(runs => {
        // debugger;
    // });
  // }
});
